This library hosts all classifiers that the user can choose from.

Two types of classifiers have to be distinguished:
1 - insite classifiers, trained on data obtained from one site only, and
2 - general classifiers, trained on data pooled from several missions.